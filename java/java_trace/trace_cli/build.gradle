plugins {
    id 'application'
}

application {
    mainClass = 'com.libkperf.tracex.cli.TraceCliMain'
}

tasks.named('jar') {
    manifest {
        attributes(
            'Main-Class': application.mainClass.get()
        )
    }
}
