project(go)

set(TARGET_INCLUDE_DIR ${CMAKE_CURRENT_LIST_DIR}/src/libkperf/include)
set(TARGET_LIB_DIR ${CMAKE_CURRENT_LIST_DIR}/src/libkperf/lib)

file(MAKE_DIRECTORY ${TARGET_INCLUDE_DIR})
file(MAKE_DIRECTORY ${TARGET_LIB_DIR})

file(GLOB HEAD_FILES "${CMAKE_INSTALL_PREFIX}/include/*")
file(GLOB SO_FILES "${CMAKE_INSTALL_PREFIX}/lib/*")

file(COPY ${HEAD_FILES} DESTINATION ${TARGET_INCLUDE_DIR})
file(COPY ${SO_FILES} DESTINATION ${TARGET_LIB_DIR})